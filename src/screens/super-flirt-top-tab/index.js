import React from 'react';
import {StyleSheet, View} from 'react-native';

const SuperFlirtTab = (props) => {

  return <View style={styles.container}></View>;
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default SuperFlirtTab;
